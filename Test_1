import numpy as np
import seaborn as sns
import pandas as pd
print("Hello world")

df = pd.read_csv("datasets.csv")
#print(datasets.head())
#print(df.groupby("dataset").mean())
#(mean_x,mean_y) = df[df["dataset"] == "dino"]["x"].mean(), df[df["dataset"] == "dino"]["y"].mean()
#print(mean_x, mean_y)

print(df.dataset.value_counts())





